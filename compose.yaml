services:
  rest-service:
    build: .
    ports:
      - "8080:8080"
    networks:
      - backend

  front:
    image: node:16
    volumes:
      - ./front:/app
    working_dir: /app
    command: ["npm", "start"]
    ports:
      - "3000:3000"
    networks:
      - backend

networks:
  backend:
